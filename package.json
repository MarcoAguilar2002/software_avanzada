{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "docker:stop": "docker stop $(docker ps -a -q)",
        "docker:dev": "docker-compose up --remove-orphans -d",
        "docker:start": "npm run docker:stop && npm run docker:dev"
    },
    "devDependencies": {
        "@popperjs/core": "^2.11.6",
        "axios": "^1.1.2",
        "bootstrap": "^5.2.3",
        "laravel-vite-plugin": "^0.8.0",
        "sass": "^1.56.1",
        "vite": "^4.0.0"
    }
}
